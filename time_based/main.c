#include <stdio.h>
#include <unistd.h>
#include <pthread.h>

void sleep_ms(int milliseconds){
    usleep(milliseconds * 1000);
}

int main(){

    int recebendo = 0;
    int recebidos[10];
    int counter = 0;
    while (recebendo == 0){

        recebidos[counter];
    }

    return 0;
}
