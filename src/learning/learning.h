#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <time.h>

void read_file(char *challange,int *challange_counter);

int compare(char *answer, char *input,
            int input_size);
